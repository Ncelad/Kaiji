@page
@using Kaiji.Pages
@model CreateRowModel
@{
    ViewData["Title"] = "Create Row";
}
<h1>@ViewData["Title"]</h1>
<div class="row">
    <div class="col-md-4">
        <section>
            <form id="account" method="post">
                <hr />
                <div class="form-group">
                    <label asp-for="NewRow.Title"></label>
                    <input name="title" asp-for="NewRow.Title" class="form-control" />
                </div>
                <div class="form-group">
                    <button type="submit" class="btn btn-primary">Create</button>
                </div>
                <div class="form-group">
                    @{Dictionary<string, string> dict = new Dictionary<string, string>() { { "id", CreateRowModel.Instance.CurrentProjectId.ToString() } };}
                    <a asp-page="./ProjectEdit" asp-all-route-data="dict">Back</a>
                </div>
            </form>
        </section>
    </div>
</div>